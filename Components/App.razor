<!DOCTYPE html>
<html lang="en" style="background-color: #282a36 !important; margin: 0; padding: 0;">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />

    <!-- Open Graph / Social Media Meta Tags -->
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="Hyunjo Jung - Dev Blog" />
    <meta property="og:title" content="Hyunjo Jung - Interactive Terminal Blog" />
    <meta property="og:description" content="Developer blog with interactive terminal interface. Explore posts using command-line commands." />
    <meta property="og:image" content="https://hyunjo.uk/og-image.jpg" />
    <meta property="og:url" content="https://hyunjo.uk" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="Hyunjo Jung - Interactive Terminal Blog" />
    <meta name="twitter:description" content="Developer blog with interactive terminal interface" />
    <meta name="twitter:image" content="https://hyunjo.uk/og-image.jpg" />

    <!-- Kakao / General Meta -->
    <meta name="description" content="Developer blog with interactive terminal interface. Explore posts using command-line commands." />
    <meta name="author" content="Hyunjo Jung" />

    <ResourcePreloader />
    <link rel="stylesheet" href="@Assets["app.css"]" />
    <link rel="stylesheet" href="@Assets["portfolio.css"]" />
    <link rel="stylesheet" href="@Assets["blog.css"]" />
    <link rel="stylesheet" href="@Assets["Portfolio.styles.css"]" />

    <ImportMap />

    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="manifest" href="/site.webmanifest" />

    <HeadOutlet />
</head>

<body style="background-color: #282a36 !important; margin: 0; padding: 0;">
    <Routes />

    <!-- Content is visible even without JavaScript (for crawlers/bots) -->
    <noscript>
        <div style="padding: 20px; background: #44475a; color: #f8f8f2; margin: 20px; border-radius: 5px;">
            This site works best with JavaScript enabled, but all content is accessible without it.
        </div>
    </noscript>

    <ReconnectModal />

    <!-- Load Blazor script asynchronously, non-blocking -->
    <!-- For bots/crawlers, content is already visible via prerendering -->
    <script data-blazor-src="@Assets["_framework/blazor.web.js"]">
        // Detect if user is a bot/crawler - if so, skip Blazor loading for faster response
        var ua = navigator.userAgent.toLowerCase();
        var isBot = /bot|crawler|spider|crawling|googlebot|bingbot|slurp|duckduckbot|baiduspider|yandexbot|sogou|exabot|facebot|ia_archiver|ChatGPT|Claude|Perplexity/i.test(ua);

        if (!isBot) {
            // Load Blazor only for real users
            var script = document.createElement('script');
            var blazorSrc = document.currentScript.getAttribute('data-blazor-src');
            script.src = blazorSrc;
            script.defer = true;
            document.body.appendChild(script);
        }
    </script>
</body>

</html>
